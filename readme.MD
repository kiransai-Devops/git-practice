# git reset : changes are undone
* it have three types of reset options
* soft,mixed,hard
* in soft,changes are show in staging area.not deleted in locally (git reset --soft HEAD~1) it remove the first line
* in mixed,changes are not staging.not deleted in locally also 
* in hard, changes are showing in staging area. also deleted in locally

